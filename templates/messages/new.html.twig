{% extends 'base.html.twig' %}

{% block body %}
    <h1>Contact</h1>
    <div id="contact">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" formnovalidate>Send</button>
        {{ form_end(form) }}
    </div>
{% endblock %}

{% block title %}
    Contact
{% endblock %}

